# wellqc
A web application that ingests well-log (.LAS) files, standardises and validates  curve data against petrophysical bounds, scores data quality, imputes missing  values, and produces exportable audit report
